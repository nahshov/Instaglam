import React from 'react';
import Spinner from 'assets/img/spinner.gif';

const LoadingSpinner = () => (
  <img src={Spinner} alt="spinner" style={{ width: '30px', height: '30px' }} />
);

export default LoadingSpinner;
