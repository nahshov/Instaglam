import React from 'react';
import Spinner from 'assets/img/spinner.gif';

const LoadingSpinner = () => (
  <img src={Spinner} alt="spinner" style={{ width: '20%', height: '20%' }} />
);

export default LoadingSpinner;
